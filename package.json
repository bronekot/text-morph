{
  "name": "text-morph",
  "private": true,
  "version": "0.1.0",
  "description": "Framework-independent JavaScript text morphing utilities.",
  "type": "module",
  "main": "./src/index.js",
  "module": "./src/index.js",
  "exports": {
    ".": "./src/index.js"
  },
  "files": [
    "src",
    "dist",
    "README.md"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "vite build",
    "check": "node --input-type=module -e \"import('./src/index.js').then(({ createTextMorphSteps }) => { const result = createTextMorphSteps('abc', 'abd', { seed: 1 }); if (!result.steps.length) process.exit(1); })\""
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
